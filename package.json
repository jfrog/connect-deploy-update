{
  "name": "connect-deploy-update",
  "version": "1.0.0",
  "description": "This GitHub Action allows you to deploy an update flow configured in JFrog Connect’s web UI to specific devices or groups of devices within your fleet. You can specify device filters, update configurations, and provide deployment comments.",
  "main": "index.js",
  "scripts": {
    "pack": "esbuild src/index.js --bundle --platform=node --outfile=bin/run.js",
    "test": "jest"
  },
  "keywords": [],
  "author": "JFrog",
  "license": "",
  "devDependencies": {
    "esbuild": "0.25.0",
    "jest": "^29.7.0",
    "nock": "^14.0.1"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "axios": "^1.11.0"
  }
}
